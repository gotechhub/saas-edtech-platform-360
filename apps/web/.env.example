# Demo has no authentication and must not contain real personal data.
# Explicitly enable only on a dedicated demo deployment.
RESPONGO_DEMO_ENABLED=false
# Production identity integration is a later EP02 task. Do not insert service keys in NEXT_PUBLIC variables.
